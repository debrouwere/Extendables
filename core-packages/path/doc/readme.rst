=============================================
``path``: handling paths to files and folders
=============================================

The ``path`` module provides a few common helpers for handling file paths. It will probably be rewritten at some point as methods on the ``File`` and ``Folder`` objects.

.. include:: jsdoc/_global_.rst